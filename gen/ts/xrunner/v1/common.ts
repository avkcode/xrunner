// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.2
// source: xrunner/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "xrunner.v1";

export enum JobState {
  JOB_STATE_UNSPECIFIED = 0,
  JOB_STATE_ACCEPTED = 1,
  JOB_STATE_RUNNING = 2,
  JOB_STATE_SUCCEEDED = 3,
  JOB_STATE_FAILED = 4,
  UNRECOGNIZED = -1,
}

export function jobStateFromJSON(object: any): JobState {
  switch (object) {
    case 0:
    case "JOB_STATE_UNSPECIFIED":
      return JobState.JOB_STATE_UNSPECIFIED;
    case 1:
    case "JOB_STATE_ACCEPTED":
      return JobState.JOB_STATE_ACCEPTED;
    case 2:
    case "JOB_STATE_RUNNING":
      return JobState.JOB_STATE_RUNNING;
    case 3:
    case "JOB_STATE_SUCCEEDED":
      return JobState.JOB_STATE_SUCCEEDED;
    case 4:
    case "JOB_STATE_FAILED":
      return JobState.JOB_STATE_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return JobState.UNRECOGNIZED;
  }
}

export function jobStateToJSON(object: JobState): string {
  switch (object) {
    case JobState.JOB_STATE_UNSPECIFIED:
      return "JOB_STATE_UNSPECIFIED";
    case JobState.JOB_STATE_ACCEPTED:
      return "JOB_STATE_ACCEPTED";
    case JobState.JOB_STATE_RUNNING:
      return "JOB_STATE_RUNNING";
    case JobState.JOB_STATE_SUCCEEDED:
      return "JOB_STATE_SUCCEEDED";
    case JobState.JOB_STATE_FAILED:
      return "JOB_STATE_FAILED";
    case JobState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum SandboxType {
  SANDBOX_TYPE_UNSPECIFIED = 0,
  SANDBOX_TYPE_NSJAIL = 1,
  SANDBOX_TYPE_NSJAIL_DOCKER = 2,
  SANDBOX_TYPE_DOCKER = 3,
  UNRECOGNIZED = -1,
}

export function sandboxTypeFromJSON(object: any): SandboxType {
  switch (object) {
    case 0:
    case "SANDBOX_TYPE_UNSPECIFIED":
      return SandboxType.SANDBOX_TYPE_UNSPECIFIED;
    case 1:
    case "SANDBOX_TYPE_NSJAIL":
      return SandboxType.SANDBOX_TYPE_NSJAIL;
    case 2:
    case "SANDBOX_TYPE_NSJAIL_DOCKER":
      return SandboxType.SANDBOX_TYPE_NSJAIL_DOCKER;
    case 3:
    case "SANDBOX_TYPE_DOCKER":
      return SandboxType.SANDBOX_TYPE_DOCKER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SandboxType.UNRECOGNIZED;
  }
}

export function sandboxTypeToJSON(object: SandboxType): string {
  switch (object) {
    case SandboxType.SANDBOX_TYPE_UNSPECIFIED:
      return "SANDBOX_TYPE_UNSPECIFIED";
    case SandboxType.SANDBOX_TYPE_NSJAIL:
      return "SANDBOX_TYPE_NSJAIL";
    case SandboxType.SANDBOX_TYPE_NSJAIL_DOCKER:
      return "SANDBOX_TYPE_NSJAIL_DOCKER";
    case SandboxType.SANDBOX_TYPE_DOCKER:
      return "SANDBOX_TYPE_DOCKER";
    case SandboxType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface JobRef {
  tenantId: string;
  jobId: string;
}

export interface ResourceHints {
  cpuCores: number;
  memoryBytes: number;
  needsGpu: boolean;
}

export interface LogChunk {
  timestamp: Date | undefined;
  stream: string;
  data: Uint8Array;
  /**
   * Offset is a monotonically increasing per-job cursor that allows clients to
   * resume streaming without duplicating already received chunks.
   */
  offset: number;
}

export interface SandboxSpec {
  type: SandboxType;
  nsjailConfig: string;
  sandboxBin: string;
  binds: string[];
  workdir: string;
  logPath: string;
  extraArgs: string[];
}

function createBaseJobRef(): JobRef {
  return { tenantId: "", jobId: "" };
}

export const JobRef: MessageFns<JobRef> = {
  encode(message: JobRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantId !== "") {
      writer.uint32(10).string(message.tenantId);
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobRef {
    return {
      tenantId: isSet(object.tenantId) ? globalThis.String(object.tenantId) : "",
      jobId: isSet(object.jobId) ? globalThis.String(object.jobId) : "",
    };
  },

  toJSON(message: JobRef): unknown {
    const obj: any = {};
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobRef>, I>>(base?: I): JobRef {
    return JobRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobRef>, I>>(object: I): JobRef {
    const message = createBaseJobRef();
    message.tenantId = object.tenantId ?? "";
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseResourceHints(): ResourceHints {
  return { cpuCores: 0, memoryBytes: 0, needsGpu: false };
}

export const ResourceHints: MessageFns<ResourceHints> = {
  encode(message: ResourceHints, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cpuCores !== 0) {
      writer.uint32(9).double(message.cpuCores);
    }
    if (message.memoryBytes !== 0) {
      writer.uint32(16).int64(message.memoryBytes);
    }
    if (message.needsGpu !== false) {
      writer.uint32(24).bool(message.needsGpu);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceHints {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceHints();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.cpuCores = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.memoryBytes = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.needsGpu = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceHints {
    return {
      cpuCores: isSet(object.cpuCores) ? globalThis.Number(object.cpuCores) : 0,
      memoryBytes: isSet(object.memoryBytes) ? globalThis.Number(object.memoryBytes) : 0,
      needsGpu: isSet(object.needsGpu) ? globalThis.Boolean(object.needsGpu) : false,
    };
  },

  toJSON(message: ResourceHints): unknown {
    const obj: any = {};
    if (message.cpuCores !== 0) {
      obj.cpuCores = message.cpuCores;
    }
    if (message.memoryBytes !== 0) {
      obj.memoryBytes = Math.round(message.memoryBytes);
    }
    if (message.needsGpu !== false) {
      obj.needsGpu = message.needsGpu;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResourceHints>, I>>(base?: I): ResourceHints {
    return ResourceHints.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResourceHints>, I>>(object: I): ResourceHints {
    const message = createBaseResourceHints();
    message.cpuCores = object.cpuCores ?? 0;
    message.memoryBytes = object.memoryBytes ?? 0;
    message.needsGpu = object.needsGpu ?? false;
    return message;
  },
};

function createBaseLogChunk(): LogChunk {
  return { timestamp: undefined, stream: "", data: new Uint8Array(0), offset: 0 };
}

export const LogChunk: MessageFns<LogChunk> = {
  encode(message: LogChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).join();
    }
    if (message.stream !== "") {
      writer.uint32(18).string(message.stream);
    }
    if (message.data.length !== 0) {
      writer.uint32(26).bytes(message.data);
    }
    if (message.offset !== 0) {
      writer.uint32(32).uint64(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stream = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.offset = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogChunk {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      stream: isSet(object.stream) ? globalThis.String(object.stream) : "",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
    };
  },

  toJSON(message: LogChunk): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.stream !== "") {
      obj.stream = message.stream;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LogChunk>, I>>(base?: I): LogChunk {
    return LogChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LogChunk>, I>>(object: I): LogChunk {
    const message = createBaseLogChunk();
    message.timestamp = object.timestamp ?? undefined;
    message.stream = object.stream ?? "";
    message.data = object.data ?? new Uint8Array(0);
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBaseSandboxSpec(): SandboxSpec {
  return { type: 0, nsjailConfig: "", sandboxBin: "", binds: [], workdir: "", logPath: "", extraArgs: [] };
}

export const SandboxSpec: MessageFns<SandboxSpec> = {
  encode(message: SandboxSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.nsjailConfig !== "") {
      writer.uint32(18).string(message.nsjailConfig);
    }
    if (message.sandboxBin !== "") {
      writer.uint32(26).string(message.sandboxBin);
    }
    for (const v of message.binds) {
      writer.uint32(34).string(v!);
    }
    if (message.workdir !== "") {
      writer.uint32(42).string(message.workdir);
    }
    if (message.logPath !== "") {
      writer.uint32(50).string(message.logPath);
    }
    for (const v of message.extraArgs) {
      writer.uint32(58).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SandboxSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSandboxSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nsjailConfig = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sandboxBin = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.binds.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.workdir = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.logPath = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.extraArgs.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SandboxSpec {
    return {
      type: isSet(object.type) ? sandboxTypeFromJSON(object.type) : 0,
      nsjailConfig: isSet(object.nsjailConfig) ? globalThis.String(object.nsjailConfig) : "",
      sandboxBin: isSet(object.sandboxBin) ? globalThis.String(object.sandboxBin) : "",
      binds: globalThis.Array.isArray(object?.binds) ? object.binds.map((e: any) => globalThis.String(e)) : [],
      workdir: isSet(object.workdir) ? globalThis.String(object.workdir) : "",
      logPath: isSet(object.logPath) ? globalThis.String(object.logPath) : "",
      extraArgs: globalThis.Array.isArray(object?.extraArgs)
        ? object.extraArgs.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SandboxSpec): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = sandboxTypeToJSON(message.type);
    }
    if (message.nsjailConfig !== "") {
      obj.nsjailConfig = message.nsjailConfig;
    }
    if (message.sandboxBin !== "") {
      obj.sandboxBin = message.sandboxBin;
    }
    if (message.binds?.length) {
      obj.binds = message.binds;
    }
    if (message.workdir !== "") {
      obj.workdir = message.workdir;
    }
    if (message.logPath !== "") {
      obj.logPath = message.logPath;
    }
    if (message.extraArgs?.length) {
      obj.extraArgs = message.extraArgs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SandboxSpec>, I>>(base?: I): SandboxSpec {
    return SandboxSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SandboxSpec>, I>>(object: I): SandboxSpec {
    const message = createBaseSandboxSpec();
    message.type = object.type ?? 0;
    message.nsjailConfig = object.nsjailConfig ?? "";
    message.sandboxBin = object.sandboxBin ?? "";
    message.binds = object.binds?.map((e) => e) || [];
    message.workdir = object.workdir ?? "";
    message.logPath = object.logPath ?? "";
    message.extraArgs = object.extraArgs?.map((e) => e) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
