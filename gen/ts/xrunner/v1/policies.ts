// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.2
// source: xrunner/v1/policies.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "xrunner.v1";

export interface PolicyOverrides {
  allowNetwork: boolean;
  networkProfile: string;
  maxExecutionSeconds: number;
}

function createBasePolicyOverrides(): PolicyOverrides {
  return { allowNetwork: false, networkProfile: "", maxExecutionSeconds: 0 };
}

export const PolicyOverrides: MessageFns<PolicyOverrides> = {
  encode(message: PolicyOverrides, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.allowNetwork !== false) {
      writer.uint32(8).bool(message.allowNetwork);
    }
    if (message.networkProfile !== "") {
      writer.uint32(18).string(message.networkProfile);
    }
    if (message.maxExecutionSeconds !== 0) {
      writer.uint32(24).int64(message.maxExecutionSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolicyOverrides {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicyOverrides();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.allowNetwork = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.networkProfile = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxExecutionSeconds = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolicyOverrides {
    return {
      allowNetwork: isSet(object.allowNetwork) ? globalThis.Boolean(object.allowNetwork) : false,
      networkProfile: isSet(object.networkProfile) ? globalThis.String(object.networkProfile) : "",
      maxExecutionSeconds: isSet(object.maxExecutionSeconds) ? globalThis.Number(object.maxExecutionSeconds) : 0,
    };
  },

  toJSON(message: PolicyOverrides): unknown {
    const obj: any = {};
    if (message.allowNetwork !== false) {
      obj.allowNetwork = message.allowNetwork;
    }
    if (message.networkProfile !== "") {
      obj.networkProfile = message.networkProfile;
    }
    if (message.maxExecutionSeconds !== 0) {
      obj.maxExecutionSeconds = Math.round(message.maxExecutionSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolicyOverrides>, I>>(base?: I): PolicyOverrides {
    return PolicyOverrides.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolicyOverrides>, I>>(object: I): PolicyOverrides {
    const message = createBasePolicyOverrides();
    message.allowNetwork = object.allowNetwork ?? false;
    message.networkProfile = object.networkProfile ?? "";
    message.maxExecutionSeconds = object.maxExecutionSeconds ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
