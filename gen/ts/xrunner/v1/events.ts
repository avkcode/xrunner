// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.2
// source: xrunner/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { JobRef, LogChunk } from "./common";
import { Job } from "./jobs";

export const protobufPackage = "xrunner.v1";

export interface JobEvent {
  job: Job | undefined;
  version: number;
  emittedAt: Date | undefined;
}

export interface LogEvent {
  ref: JobRef | undefined;
  chunk: LogChunk | undefined;
  offset: number;
  emittedAt: Date | undefined;
}

function createBaseJobEvent(): JobEvent {
  return { job: undefined, version: 0, emittedAt: undefined };
}

export const JobEvent: MessageFns<JobEvent> = {
  encode(message: JobEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.job !== undefined) {
      Job.encode(message.job, writer.uint32(10).fork()).join();
    }
    if (message.version !== 0) {
      writer.uint32(16).uint64(message.version);
    }
    if (message.emittedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.emittedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.job = Job.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.emittedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobEvent {
    return {
      job: isSet(object.job) ? Job.fromJSON(object.job) : undefined,
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      emittedAt: isSet(object.emittedAt) ? fromJsonTimestamp(object.emittedAt) : undefined,
    };
  },

  toJSON(message: JobEvent): unknown {
    const obj: any = {};
    if (message.job !== undefined) {
      obj.job = Job.toJSON(message.job);
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.emittedAt !== undefined) {
      obj.emittedAt = message.emittedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobEvent>, I>>(base?: I): JobEvent {
    return JobEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobEvent>, I>>(object: I): JobEvent {
    const message = createBaseJobEvent();
    message.job = (object.job !== undefined && object.job !== null) ? Job.fromPartial(object.job) : undefined;
    message.version = object.version ?? 0;
    message.emittedAt = object.emittedAt ?? undefined;
    return message;
  },
};

function createBaseLogEvent(): LogEvent {
  return { ref: undefined, chunk: undefined, offset: 0, emittedAt: undefined };
}

export const LogEvent: MessageFns<LogEvent> = {
  encode(message: LogEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ref !== undefined) {
      JobRef.encode(message.ref, writer.uint32(10).fork()).join();
    }
    if (message.chunk !== undefined) {
      LogChunk.encode(message.chunk, writer.uint32(18).fork()).join();
    }
    if (message.offset !== 0) {
      writer.uint32(24).uint64(message.offset);
    }
    if (message.emittedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.emittedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ref = JobRef.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunk = LogChunk.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.offset = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.emittedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogEvent {
    return {
      ref: isSet(object.ref) ? JobRef.fromJSON(object.ref) : undefined,
      chunk: isSet(object.chunk) ? LogChunk.fromJSON(object.chunk) : undefined,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      emittedAt: isSet(object.emittedAt) ? fromJsonTimestamp(object.emittedAt) : undefined,
    };
  },

  toJSON(message: LogEvent): unknown {
    const obj: any = {};
    if (message.ref !== undefined) {
      obj.ref = JobRef.toJSON(message.ref);
    }
    if (message.chunk !== undefined) {
      obj.chunk = LogChunk.toJSON(message.chunk);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.emittedAt !== undefined) {
      obj.emittedAt = message.emittedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LogEvent>, I>>(base?: I): LogEvent {
    return LogEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LogEvent>, I>>(object: I): LogEvent {
    const message = createBaseLogEvent();
    message.ref = (object.ref !== undefined && object.ref !== null) ? JobRef.fromPartial(object.ref) : undefined;
    message.chunk = (object.chunk !== undefined && object.chunk !== null)
      ? LogChunk.fromPartial(object.chunk)
      : undefined;
    message.offset = object.offset ?? 0;
    message.emittedAt = object.emittedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
