// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.2
// source: xrunner/v1/workers.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "../../google/protobuf/timestamp";
import { JobRef, JobState, jobStateFromJSON, jobStateToJSON, LogChunk, ResourceHints, SandboxSpec } from "./common";
import { Workload } from "./jobs";
import { PolicyOverrides } from "./policies";

export const protobufPackage = "xrunner.v1";

export interface WorkerRegistration {
  workerId: string;
  address: string;
  capabilities: string[];
}

export interface RegisterWorkerRequest {
  registration: WorkerRegistration | undefined;
}

export interface RegisterWorkerResponse {
  registeredAt: Date | undefined;
}

export interface JobAssignment {
  ref: JobRef | undefined;
  workload: Workload | undefined;
  env: { [key: string]: string };
  policy: PolicyOverrides | undefined;
  resource: ResourceHints | undefined;
  sandbox: SandboxSpec | undefined;
}

export interface JobAssignment_EnvEntry {
  key: string;
  value: string;
}

export interface RunJobResponse {
  ref: JobRef | undefined;
  finalState: JobState;
  exitCode: number;
  statusMessage: string;
  startedAt: Date | undefined;
  completedAt: Date | undefined;
  logs: LogChunk[];
  terminal: boolean;
}

function createBaseWorkerRegistration(): WorkerRegistration {
  return { workerId: "", address: "", capabilities: [] };
}

export const WorkerRegistration: MessageFns<WorkerRegistration> = {
  encode(message: WorkerRegistration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workerId !== "") {
      writer.uint32(10).string(message.workerId);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    for (const v of message.capabilities) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkerRegistration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkerRegistration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.capabilities.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkerRegistration {
    return {
      workerId: isSet(object.workerId) ? globalThis.String(object.workerId) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: WorkerRegistration): unknown {
    const obj: any = {};
    if (message.workerId !== "") {
      obj.workerId = message.workerId;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkerRegistration>, I>>(base?: I): WorkerRegistration {
    return WorkerRegistration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkerRegistration>, I>>(object: I): WorkerRegistration {
    const message = createBaseWorkerRegistration();
    message.workerId = object.workerId ?? "";
    message.address = object.address ?? "";
    message.capabilities = object.capabilities?.map((e) => e) || [];
    return message;
  },
};

function createBaseRegisterWorkerRequest(): RegisterWorkerRequest {
  return { registration: undefined };
}

export const RegisterWorkerRequest: MessageFns<RegisterWorkerRequest> = {
  encode(message: RegisterWorkerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registration !== undefined) {
      WorkerRegistration.encode(message.registration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registration = WorkerRegistration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterWorkerRequest {
    return { registration: isSet(object.registration) ? WorkerRegistration.fromJSON(object.registration) : undefined };
  },

  toJSON(message: RegisterWorkerRequest): unknown {
    const obj: any = {};
    if (message.registration !== undefined) {
      obj.registration = WorkerRegistration.toJSON(message.registration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterWorkerRequest>, I>>(base?: I): RegisterWorkerRequest {
    return RegisterWorkerRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkerRequest>, I>>(object: I): RegisterWorkerRequest {
    const message = createBaseRegisterWorkerRequest();
    message.registration = (object.registration !== undefined && object.registration !== null)
      ? WorkerRegistration.fromPartial(object.registration)
      : undefined;
    return message;
  },
};

function createBaseRegisterWorkerResponse(): RegisterWorkerResponse {
  return { registeredAt: undefined };
}

export const RegisterWorkerResponse: MessageFns<RegisterWorkerResponse> = {
  encode(message: RegisterWorkerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registeredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.registeredAt), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registeredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterWorkerResponse {
    return { registeredAt: isSet(object.registeredAt) ? fromJsonTimestamp(object.registeredAt) : undefined };
  },

  toJSON(message: RegisterWorkerResponse): unknown {
    const obj: any = {};
    if (message.registeredAt !== undefined) {
      obj.registeredAt = message.registeredAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterWorkerResponse>, I>>(base?: I): RegisterWorkerResponse {
    return RegisterWorkerResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkerResponse>, I>>(object: I): RegisterWorkerResponse {
    const message = createBaseRegisterWorkerResponse();
    message.registeredAt = object.registeredAt ?? undefined;
    return message;
  },
};

function createBaseJobAssignment(): JobAssignment {
  return { ref: undefined, workload: undefined, env: {}, policy: undefined, resource: undefined, sandbox: undefined };
}

export const JobAssignment: MessageFns<JobAssignment> = {
  encode(message: JobAssignment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ref !== undefined) {
      JobRef.encode(message.ref, writer.uint32(10).fork()).join();
    }
    if (message.workload !== undefined) {
      Workload.encode(message.workload, writer.uint32(18).fork()).join();
    }
    Object.entries(message.env).forEach(([key, value]) => {
      JobAssignment_EnvEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.policy !== undefined) {
      PolicyOverrides.encode(message.policy, writer.uint32(34).fork()).join();
    }
    if (message.resource !== undefined) {
      ResourceHints.encode(message.resource, writer.uint32(42).fork()).join();
    }
    if (message.sandbox !== undefined) {
      SandboxSpec.encode(message.sandbox, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobAssignment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ref = JobRef.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workload = Workload.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = JobAssignment_EnvEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.env[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.policy = PolicyOverrides.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resource = ResourceHints.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sandbox = SandboxSpec.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobAssignment {
    return {
      ref: isSet(object.ref) ? JobRef.fromJSON(object.ref) : undefined,
      workload: isSet(object.workload) ? Workload.fromJSON(object.workload) : undefined,
      env: isObject(object.env)
        ? Object.entries(object.env).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      policy: isSet(object.policy) ? PolicyOverrides.fromJSON(object.policy) : undefined,
      resource: isSet(object.resource) ? ResourceHints.fromJSON(object.resource) : undefined,
      sandbox: isSet(object.sandbox) ? SandboxSpec.fromJSON(object.sandbox) : undefined,
    };
  },

  toJSON(message: JobAssignment): unknown {
    const obj: any = {};
    if (message.ref !== undefined) {
      obj.ref = JobRef.toJSON(message.ref);
    }
    if (message.workload !== undefined) {
      obj.workload = Workload.toJSON(message.workload);
    }
    if (message.env) {
      const entries = Object.entries(message.env);
      if (entries.length > 0) {
        obj.env = {};
        entries.forEach(([k, v]) => {
          obj.env[k] = v;
        });
      }
    }
    if (message.policy !== undefined) {
      obj.policy = PolicyOverrides.toJSON(message.policy);
    }
    if (message.resource !== undefined) {
      obj.resource = ResourceHints.toJSON(message.resource);
    }
    if (message.sandbox !== undefined) {
      obj.sandbox = SandboxSpec.toJSON(message.sandbox);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobAssignment>, I>>(base?: I): JobAssignment {
    return JobAssignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobAssignment>, I>>(object: I): JobAssignment {
    const message = createBaseJobAssignment();
    message.ref = (object.ref !== undefined && object.ref !== null) ? JobRef.fromPartial(object.ref) : undefined;
    message.workload = (object.workload !== undefined && object.workload !== null)
      ? Workload.fromPartial(object.workload)
      : undefined;
    message.env = Object.entries(object.env ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? PolicyOverrides.fromPartial(object.policy)
      : undefined;
    message.resource = (object.resource !== undefined && object.resource !== null)
      ? ResourceHints.fromPartial(object.resource)
      : undefined;
    message.sandbox = (object.sandbox !== undefined && object.sandbox !== null)
      ? SandboxSpec.fromPartial(object.sandbox)
      : undefined;
    return message;
  },
};

function createBaseJobAssignment_EnvEntry(): JobAssignment_EnvEntry {
  return { key: "", value: "" };
}

export const JobAssignment_EnvEntry: MessageFns<JobAssignment_EnvEntry> = {
  encode(message: JobAssignment_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobAssignment_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobAssignment_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobAssignment_EnvEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: JobAssignment_EnvEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobAssignment_EnvEntry>, I>>(base?: I): JobAssignment_EnvEntry {
    return JobAssignment_EnvEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobAssignment_EnvEntry>, I>>(object: I): JobAssignment_EnvEntry {
    const message = createBaseJobAssignment_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseRunJobResponse(): RunJobResponse {
  return {
    ref: undefined,
    finalState: 0,
    exitCode: 0,
    statusMessage: "",
    startedAt: undefined,
    completedAt: undefined,
    logs: [],
    terminal: false,
  };
}

export const RunJobResponse: MessageFns<RunJobResponse> = {
  encode(message: RunJobResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ref !== undefined) {
      JobRef.encode(message.ref, writer.uint32(10).fork()).join();
    }
    if (message.finalState !== 0) {
      writer.uint32(16).int32(message.finalState);
    }
    if (message.exitCode !== 0) {
      writer.uint32(24).int32(message.exitCode);
    }
    if (message.statusMessage !== "") {
      writer.uint32(34).string(message.statusMessage);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(42).fork()).join();
    }
    if (message.completedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.completedAt), writer.uint32(50).fork()).join();
    }
    for (const v of message.logs) {
      LogChunk.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.terminal !== false) {
      writer.uint32(64).bool(message.terminal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunJobResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunJobResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ref = JobRef.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.finalState = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.completedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.logs.push(LogChunk.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.terminal = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunJobResponse {
    return {
      ref: isSet(object.ref) ? JobRef.fromJSON(object.ref) : undefined,
      finalState: isSet(object.finalState) ? jobStateFromJSON(object.finalState) : 0,
      exitCode: isSet(object.exitCode) ? globalThis.Number(object.exitCode) : 0,
      statusMessage: isSet(object.statusMessage) ? globalThis.String(object.statusMessage) : "",
      startedAt: isSet(object.startedAt) ? fromJsonTimestamp(object.startedAt) : undefined,
      completedAt: isSet(object.completedAt) ? fromJsonTimestamp(object.completedAt) : undefined,
      logs: globalThis.Array.isArray(object?.logs) ? object.logs.map((e: any) => LogChunk.fromJSON(e)) : [],
      terminal: isSet(object.terminal) ? globalThis.Boolean(object.terminal) : false,
    };
  },

  toJSON(message: RunJobResponse): unknown {
    const obj: any = {};
    if (message.ref !== undefined) {
      obj.ref = JobRef.toJSON(message.ref);
    }
    if (message.finalState !== 0) {
      obj.finalState = jobStateToJSON(message.finalState);
    }
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    if (message.statusMessage !== "") {
      obj.statusMessage = message.statusMessage;
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.completedAt !== undefined) {
      obj.completedAt = message.completedAt.toISOString();
    }
    if (message.logs?.length) {
      obj.logs = message.logs.map((e) => LogChunk.toJSON(e));
    }
    if (message.terminal !== false) {
      obj.terminal = message.terminal;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunJobResponse>, I>>(base?: I): RunJobResponse {
    return RunJobResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunJobResponse>, I>>(object: I): RunJobResponse {
    const message = createBaseRunJobResponse();
    message.ref = (object.ref !== undefined && object.ref !== null) ? JobRef.fromPartial(object.ref) : undefined;
    message.finalState = object.finalState ?? 0;
    message.exitCode = object.exitCode ?? 0;
    message.statusMessage = object.statusMessage ?? "";
    message.startedAt = object.startedAt ?? undefined;
    message.completedAt = object.completedAt ?? undefined;
    message.logs = object.logs?.map((e) => LogChunk.fromPartial(e)) || [];
    message.terminal = object.terminal ?? false;
    return message;
  },
};

export type WorkerServiceService = typeof WorkerServiceService;
export const WorkerServiceService = {
  runJob: {
    path: "/xrunner.v1.WorkerService/RunJob",
    requestStream: false,
    responseStream: true,
    requestSerialize: (value: JobAssignment): Buffer => Buffer.from(JobAssignment.encode(value).finish()),
    requestDeserialize: (value: Buffer): JobAssignment => JobAssignment.decode(value),
    responseSerialize: (value: RunJobResponse): Buffer => Buffer.from(RunJobResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RunJobResponse => RunJobResponse.decode(value),
  },
} as const;

export interface WorkerServiceServer extends UntypedServiceImplementation {
  runJob: handleServerStreamingCall<JobAssignment, RunJobResponse>;
}

export interface WorkerServiceClient extends Client {
  runJob(request: JobAssignment, options?: Partial<CallOptions>): ClientReadableStream<RunJobResponse>;
  runJob(
    request: JobAssignment,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<RunJobResponse>;
}

export const WorkerServiceClient = makeGenericClientConstructor(
  WorkerServiceService,
  "xrunner.v1.WorkerService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): WorkerServiceClient;
  service: typeof WorkerServiceService;
  serviceName: string;
};

export type WorkerRegistryServiceService = typeof WorkerRegistryServiceService;
export const WorkerRegistryServiceService = {
  registerWorker: {
    path: "/xrunner.v1.WorkerRegistryService/RegisterWorker",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: RegisterWorkerRequest): Buffer =>
      Buffer.from(RegisterWorkerRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterWorkerRequest => RegisterWorkerRequest.decode(value),
    responseSerialize: (value: RegisterWorkerResponse): Buffer =>
      Buffer.from(RegisterWorkerResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterWorkerResponse => RegisterWorkerResponse.decode(value),
  },
} as const;

export interface WorkerRegistryServiceServer extends UntypedServiceImplementation {
  registerWorker: handleUnaryCall<RegisterWorkerRequest, RegisterWorkerResponse>;
}

export interface WorkerRegistryServiceClient extends Client {
  registerWorker(
    request: RegisterWorkerRequest,
    callback: (error: ServiceError | null, response: RegisterWorkerResponse) => void,
  ): ClientUnaryCall;
  registerWorker(
    request: RegisterWorkerRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterWorkerResponse) => void,
  ): ClientUnaryCall;
  registerWorker(
    request: RegisterWorkerRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterWorkerResponse) => void,
  ): ClientUnaryCall;
}

export const WorkerRegistryServiceClient = makeGenericClientConstructor(
  WorkerRegistryServiceService,
  "xrunner.v1.WorkerRegistryService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): WorkerRegistryServiceClient;
  service: typeof WorkerRegistryServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
